from typing import Any

def require_version(namespace: str, version: str) -> None:
    """Set a requirement for namespace to be introspected.

    Args:
        namespace: GI namespace to require (e.g. 'Gtk')
        version: Version to require (e.g. '4.0')
    """
    pass